<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AnimPopupDir">
        <item name="android:windowEnterAnimation">@anim/slide_in</item>
        <item name="android:windowExitAnimation">@anim/slide_out</item>
    </style>

    <style name="ProgressDialog" parent="@android:style/Theme.Holo.Dialog">
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowFrame">@null</item>
    </style>

    <style name="SwipeBackLayout">
        <item name="edge_size">50dip</item>
        <item name="shadow_left">@mipmap/shadow_left</item>
        <item name="shadow_right">@mipmap/shadow_right</item>
        <item name="shadow_bottom">@mipmap/shadow_bottom</item>
    </style>


    <!-- actionbar的overflow-->
    <style name="ActionButton.Overflow.Light" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:src">@mipmap/ic_menu_overflow</item>
    </style>

    <style name="OverflowMenuStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
        <item name="android:popupBackground">@drawable/abc_popup_background_mtrl_mult</item>
    </style>

    <!-- search_view-->
    <style name="SearchView.Light" parent="Base.Widget.AppCompat.SearchView">
        <item name="queryBackground">@drawable/selector_textfield_search_material</item>
        <item name="submitBackground">@drawable/selector_textfield_search_material</item>
        <item name="closeIcon">@mipmap/ic_clear_mtrl_alpha</item>
        <item name="searchIcon">@mipmap/ic_menu_search</item>
        <item name="searchHintIcon">@null</item>
        <item name="goIcon">@mipmap/ic_go_search</item>
    </style>

    <style name="Widget.ProgressBar.Red" parent="Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/progress_medium</item>
    </style>

    <style name="Widget.ProgressBar.Horizontal.New" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/progress_horizontal_new</item>
        <item name="android:minHeight">4dp</item>
    </style>

    <style name="Widget.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextAppearance">@style/Widget.Toolbar.Title</item>
    </style>

    <style name="Widget.Toolbar.Title" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="WindowAnimationStyle" parent="android:Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/activity_open_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/activity_open_exit</item>
        <item name="android:activityCloseEnterAnimation">@anim/activity_close_enter</item>
        <item name="android:activityCloseExitAnimation">@anim/activity_close_exit</item>
    </style>

    <style name="Widget.TextView.Style" parent="android:Widget.TextView">
        <item name="android:textCursorDrawable">@null</item>
    </style>

    <!-- 横向分割线-->
    <style name="HorizontalLine">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/normal_light_grey</item>
    </style>
    <!-- 定长横向分割线-->
    <style name="FixedHorizontalLine">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_marginRight">64dp</item>
        <item name="android:layout_marginLeft">64dp</item>
        <item name="android:background">?attr/colorPrimary</item>
    </style>
    <!-- 纵向分割线-->
    <style name="VerticalLine">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/normal_light_grey</item>
    </style>
    <!-- listView 标准样式 -->
    <style name="ListViewStyle">
        <!--<item name="android:layout_width">match_parent</item>-->
        <!--<item name="android:layout_height">match_parent</item>-->
        <item name="android:cacheColorHint">@color/transparent</item>
        <item name="android:divider">@color/normal_light_grey</item>
        <item name="android:fadingEdge">none</item>
        <item name="android:listSelector">@drawable/selector_list_item_gray</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:scrollbars">none</item>
    </style>

    <!-- 日期类型文本样式 -->
    <style name="DateTextStyle">
        <item name="android:textColor">@color/normal_grey</item>
        <item name="android:textSize">@dimen/text_small</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>